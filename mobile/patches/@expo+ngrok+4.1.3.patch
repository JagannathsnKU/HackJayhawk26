diff --git a/node_modules/@expo/ngrok/src/client.js b/node_modules/@expo/ngrok/src/client.js
index 3f748a3..9fe57c2 100644
--- a/node_modules/@expo/ngrok/src/client.js
+++ b/node_modules/@expo/ngrok/src/client.js
@@ -29,6 +29,9 @@ class NgrokClient {
     } catch (error) {
       let clientError;
       try {
+        if (!error.response || error.response.body == null) {
+          throw error;
+        }
         const response = JSON.parse(error.response.body);
         clientError = new NgrokClientError(
           response.msg,
@@ -36,10 +39,14 @@ class NgrokClient {
           response
         );
       } catch (e) {
+        if (!error.response) {
+          throw error;
+        }
+        const rawBody = error.response.body;
         clientError = new NgrokClientError(
-          error.response.body,
+          rawBody != null ? String(rawBody) : error.message || "Ngrok request failed",
           error.response,
-          error.response.body
+          rawBody
         );
       }
       throw clientError;
@@ -52,6 +59,9 @@ class NgrokClient {
         (response) => response.statusCode === 204
       );
     } catch (error) {
+      if (!error.response || error.response.body == null) {
+        throw error;
+      }
       const response = JSON.parse(error.response.body);
       throw new NgrokClientError(response.msg, error.response, response);
     }
diff --git a/node_modules/@expo/ngrok/src/utils.js b/node_modules/@expo/ngrok/src/utils.js
index 1c2288a..db0d8b1 100644
--- a/node_modules/@expo/ngrok/src/utils.js
+++ b/node_modules/@expo/ngrok/src/utils.js
@@ -38,7 +38,14 @@ function isRetriable(err) {
   }
   const statusCode = err.response.statusCode;
   const body = err.body;
-  const notReady500 = statusCode === 500 && /panic/.test(body);
+  if (body == null) {
+    return false;
+  }
+  if (typeof body !== "object") {
+    const notReady500 = statusCode === 500 && /panic/.test(String(body));
+    return notReady500;
+  }
+  const notReady500 = statusCode === 500 && /panic/.test(String(body.msg || body));
   const notReady502 =
     statusCode === 502 &&
     body.details &&
