export type AnomalousMatterHeroProps = {
  title?: string;
  subtitle?: string;
  description?: string;
  /** Smaller orb on welcome — passed to GL mesh. */
  meshRadius?: number;
  cameraZ?: number;
  fov?: number;
};
