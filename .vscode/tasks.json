{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Expo (tunnel — Expo Go)",
      "type": "shell",
      "command": "npm run start",
      "options": { "cwd": "${workspaceFolder}/mobile" },
      "group": "none",
      "presentation": { "reveal": "always", "panel": "dedicated", "focus": true }
    },
    {
      "label": "Expo (web + tunnel)",
      "type": "shell",
      "command": "npm run web",
      "options": { "cwd": "${workspaceFolder}/mobile" },
      "group": "none",
      "presentation": { "reveal": "always", "panel": "dedicated", "focus": true }
    }
  ]
}
